# Imports:
from django.conf.urls import patterns, include, url

urlpatterns = patterns('directorys.views',
    # Examples:
    # url(r'^$', 'draoo.views.home', name='home'),
    #url(r'^$', 'index', name='index'),
    #url(r'^directorys/$', 'directorys', name='directorys'),
)